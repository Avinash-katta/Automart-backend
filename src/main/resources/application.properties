# Server Settings
server.port=8080
server.servlet.context-path=/

# Database Configuration (MySQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/e-commerce?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Avinash@05}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
# Set to 'none' to avoid modifying the user's existing database schema
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Gmail SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=avinashkatta094@gmail.com
spring.mail.password=goue tixe okxu vkbc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# App Security JWT Configuration
# Hex-encoded 256-bit key
app.jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
app.jwt.expiration-milliseconds=86400000
app.reset-token-expiration-minutes=15
app.frontend-url=${FRONTEND_URL:http://localhost:5173}

# Razorpay Configuration
razorpay.key.id=rzp_test_TKxeuHwECuglYX
razorpay.key.secret=${RAZORPAY_API_SECRET:1eLMzlGZ1bY7Lc96E10w3nHY}

